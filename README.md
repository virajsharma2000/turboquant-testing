turboquant is a latest algorithm published by google for quantiing vectors. Due tto high precision decimal points, huge numbers in transformers model the vectors consume a lot of memory, google invented a special algorithm which uses orthogonal rotation and lyon's quantizing algorithm. 0xSero repo created a new library which impliments that algorithm which I used in this repo, it has a lot of memory cosumption loss and loss of information is extremely tiny.
